<?php 

    $numbers = [1,2,3,4,5];

    //1. count($array) => count the no: of elements in array

    
    // echo count($numbers);
    // echo "<br>";

    //2. is_array($array) => 1 if true, empty if false 
    // echo  is_array($numbers);

    //3. in_array($element, $arr)
    //echo in_array(5,$numbers);


    //4. array_search($element,$arr) => return index of the element if fount,
    // empty if false 

    // echo array_search(5,$numbers);

    //array_push($arr,$element,...) => add more elements at the end of the array
    // array_push($numbers,6,7,8);
    // print_r($numbers);

    //array_unshift($arr,$element,...) => add more elements at the beginning of the array 
    // array_unshift($numbers,20);
    // print_r($numbers);


    //array_pop => remove the last element of the array 
    //   echo array_pop($numbers);

    //array_shift($array) => remove the first element of the array 
    // echo array_shift($numbers);





?>